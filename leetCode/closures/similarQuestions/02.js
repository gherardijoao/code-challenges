/*Create a function createSecret(value) that returns two functions: getSecret() and setSecret(newValue), and allows reading and changing the secret value — but only through the returned methods.*/

